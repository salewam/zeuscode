# Product Form — crm

## 1. Идентичность

| Поле | Значение |
|------|----------|
| **id** | `crm` |
| **title_ru** | CRM / кабинет менеджера |
| **одним предложением** | Таблица сущностей + карточка + простые статусы |
| **триггеры** | `crm`, `кабинет`, `сделки`, `клиентская база` |
| **когда НЕ это** | публичный лендинг без логина; чистый PDF |

## 2. Что отдаём

| Поле | Значение |
|------|----------|
| **главный артефакт** | веб-кабинет (список + карточка + фильтр) |
| **как открыть** | URL превью в Studio |
| **MVP** | 1. список 2. карточка/создание 3. смена статуса |
| **nice-to-have** | права ролей, импорт CSV |

## 3. Зоны

| Зона | path | сдаём |
|------|------|-------|
| design | `/src/design/` | да |
| frontend | `/src/frontend/` | да |
| backend | `/src/backend/` | да |
| tests | `/src/tests/` | да |

**Расширения:** `.html .css .js .py`

## 4. Команда

intake → design → frontend∥backend → tests → synth → review

## 5–11

Стек UI: HTML/CSS/JS. API: FastAPI. Данные: in-memory или sqlite на ход.  
DoD: список грузится; create работает; статус меняется; pytest happy+401/404; verify 0.  
Anti: фейковый «успех» без API; indigo AI-aesthetic; example.com в данных.  
Статус: **каркас** — добить на тестах.
